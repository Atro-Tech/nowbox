name = "openclaw"
description = "OpenClaw (AI assistant)"

[setup]
commands = [
  "which openclaw || npm install -g openclaw@latest",
  "export PATH=\"$(npm config get prefix)/bin:$PATH\" && openclaw agent"
]

[keys]
optional = []
self_auth = true
